# MySQL Master-Slave on Aliyun ECS (Docker)

> 在一台阿里云 ECS 上用 Docker Compose 快速拉起 MySQL 主从，并配置 OSS 定时备份。  
>

## ������ 一键启动
```bash
git clone https://github.com/1sin1-pxm/mysql-ms-on-aliyun-docker.git
cd mysql-ms-on-aliyun
docker-compose up -d

